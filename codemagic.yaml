workflows:
  ios-testflight-workflow:
    name: iOS Build and Publish to TestFlight
    max_build_duration: 120
    instance_type: mac_mini_m2

    integrations:
      app_store_connect: noourway

    environment:
      flutter: stable
      xcode: latest
      cocoapods: default

      ios_signing:
        provisioning_profiles:
          - DarNile_app_profile

    scripts:
      - name: Clean and Get Dependencies
        script: |
          flutter clean
          flutter pub get

      - name: Setup iOS Project and Pods
        script: |
          flutter build ios --config-only
          cd ios
          if [ ! -f "Podfile" ]; then
            pod init
          fi
          pod install --repo-update

      - name: Apply Vault Profiles
        script: xcode-project use-profiles

      - name: Build and Package IPA
        script: |
          # 1. بناء الـ Archive (إحنا عارفين إن دي شغالة)
          flutter build ios --release

          # 2. تحويل الـ Archive إلى IPA باستخدام البروفايل اللي في الخزنة
          # الأداة دي بتعرف أوتوماتيك مكان الـ Archive وبتربطه بالبروفايل
          xcode-project build-ipa --project ios/Runner.xcworkspace --scheme Runner

    artifacts:
      - build/ios/ipa/*.ipa

    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
      email:
        recipients:
          - codexus.eg@gmail.com
